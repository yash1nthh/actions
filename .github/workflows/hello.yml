name: display hello world
 
on: push

jobs:
    hello:
       runs-on: ubuntu-latest

       steps:
          - name: checkout repo
            uses: actions/checkout@v4


          - name: say hello
            run: echo "hello there!"